import 'package:flutter/material.dart';

const langDecoration1 = LinearGradient(
    colors: [Color(0xFF2F54F6), Color(0xFF15A8EC)],
    begin: Alignment.bottomRight,
    end: Alignment.topLeft);

const langDecoration2 = LinearGradient(
    colors: [Color(0xFF15A8EC), Color(0xFFA726FD)],
    begin: Alignment.bottomRight,
    end: Alignment.topLeft);
